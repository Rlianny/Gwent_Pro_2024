using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RestartData : MonoBehaviour
{
    // Start is called before the first frame update
    public void RestartDataButton()
    {
        GameData.RestartData();
    }
}